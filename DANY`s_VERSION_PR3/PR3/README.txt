
Esqueleto del Sitio de acuerdo a la estructura del profesor:

  > Notas a tener en cuenta:

	
	1) En el Dir "mysql" está la info necesaria para montar la BD (README.txt), no hace falta
		crear la BD previamente el fichero ya lo hace por si solo